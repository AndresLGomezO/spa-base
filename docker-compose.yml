services:
  # The production environment uses Google Cloud Managed services (Firestore, Tasks, Auth)
  # This file can be used for local production-like testing if needed,
  # but requires real GCP credentials and environment variables when backend services exist.

  # api:
  #   build:
  #     context: .
  #     dockerfile: apps/api/Dockerfile
  #   container_name: project-base_api
  #   restart: always
  #   ports:
  #     - '3000:3000'
  #   env_file:
  #     - ./apps/api/.env

  # worker-service:
  #   build:
  #     context: .
  #     dockerfile: apps/worker-service/Dockerfile
  #   container_name: project-base_worker_service
  #   restart: always
  #   env_file:
  #     - ./apps/worker-service/.env

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    container_name: project-base_web
    restart: always
    ports:
      - "8080:80"
    # env_file:
    #   - ./apps/web/.env
    # depends_on:
    #   - api

volumes: {}

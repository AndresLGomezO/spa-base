{
  "versionGroups": [
    {
      "label": "Ignore internal workspace packages",
      "dependencies": ["@repo/**", "@app/**", "@modules/**"],
      "isIgnored": true
    },
    {
      "label": "Ignore local package version fields",
      "dependencyTypes": ["local"],
      "isIgnored": true
    },
    {
      "label": "@repo/ui uses Vite 6 for Storybook (web uses catalog Vite 8)",
      "packages": ["@repo/ui"],
      "dependencies": ["vite"],
      "isIgnored": true
    },
    {
      "label": "Shared deps must use the pnpm catalog",
      "dependencyTypes": ["prod", "dev"],
      "policy": "catalog",
      "packages": ["**"],
      "dependencies": ["**"],
      "severity": {
        "NotUsingCatalog": "error",
        "MissingFromCatalog": "error"
      }
    }
  ],
  "semverGroups": [
    {
      "label": "Catalog entries use caret ranges",
      "dependencyTypes": ["pnpmCatalog"],
      "range": "^"
    }
  ]
}

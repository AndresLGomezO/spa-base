/** Scroll region for entity main pages — toolbar and metrics stay fixed above. */
export const entityListPageSlotClassName =
  "relative z-0 flex min-h-0 min-w-0 flex-1 flex-col overflow-y-auto overflow-x-hidden";

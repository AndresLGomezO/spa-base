# Mirror of docker.io/library/node (same image; use if Docker Hub times out)
FROM public.ecr.aws/docker/library/node:22-slim

ENV PNPM_HOME="/pnpm"
ENV PATH="$PNPM_HOME:$PATH"
RUN corepack enable

# Skip Cypress binary download (~200MB); not used during dev server startup
ENV CYPRESS_INSTALL_BINARY=0

WORKDIR /app

# Copy package.json files for web and its workspace dependencies
COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
COPY apps/web/package.json ./apps/web/
COPY apps/platform/package.json ./apps/platform/
COPY packages/shared-types/package.json ./packages/shared-types/
COPY packages/entities/package.json ./packages/entities/
COPY packages/modules/package.json ./packages/modules/
COPY packages/query-engine/package.json ./packages/query-engine/
COPY packages/ui-builder/package.json ./packages/ui-builder/
COPY packages/firestore-converters/package.json ./packages/firestore-converters/
COPY packages/hooks/package.json ./packages/hooks/
COPY packages/dynamic-entities/package.json ./packages/dynamic-entities/
COPY packages/rbac/package.json ./packages/rbac/
COPY packages/theme/package.json ./packages/theme/
COPY packages/ui/package.json ./packages/ui/
COPY packages/data-view/package.json ./packages/data-view/
COPY packages/eslint-config/package.json ./packages/eslint-config/
COPY packages/typescript-config/ ./packages/typescript-config/

# Install dependencies
RUN pnpm install

# Expose port (Vite dev server default port)
EXPOSE 5173

# Dev mode with hot reload.
# Compose anonymous volumes hide image node_modules; refresh deps on each start.
WORKDIR /app
CMD ["sh", "-c", "pnpm install --frozen-lockfile && cd apps/web && pnpm dev"]

# Copy to .env.development (gitignored) for local `pnpm --filter web dev`.
# Requires Firebase emulators: pnpm emulators + pnpm --filter api dev

VITE_ENV=dev
VITE_API_URL=http://127.0.0.1:3000

# Emulator stubs (Auth works). For browser push locally, replace ALL of these
# with Firebase Console → Project settings → Your apps → Web app values from the
# same project as the VAPID key (VITE_FIREBASE_API_KEY must not be fake-api-key).
# Docker Compose reads the same vars from the repo-root .env file.
VITE_FIREBASE_API_KEY=fake-api-key
VITE_FIREBASE_AUTH_DOMAIN=localhost
VITE_FIREBASE_PROJECT_ID=demo-project-base
VITE_FIREBASE_STORAGE_BUCKET=demo-project-base.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abcdef
# Web Push certificate key from Firebase Console → Cloud Messaging → Web Push certificates.
# VITE_FIREBASE_VAPID_KEY=

# Opt in to Auth + App Check emulator stubs (API must set FIREBASE_AUTH_EMULATOR_HOST).
VITE_FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099

# Optional: hide the Auth emulator "Running in emulator mode" banner (local only).
# VITE_FIREBASE_AUTH_EMULATOR_DISABLE_WARNINGS=true

# Optional: test real App Check locally without Auth emulator (Firebase Console debug token).
# VITE_APP_CHECK_RECAPTCHA_SITE_KEY=
# VITE_FIREBASE_APPCHECK_DEBUG_TOKEN=

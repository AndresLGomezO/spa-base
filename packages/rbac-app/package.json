{
  "name": "@repo/rbac-app",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "format:check": "prettier --check .",
    "validate": "pnpm run typecheck && pnpm run test && pnpm run format:check"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@repo/rbac-base": "workspace:*",
    "@repo/rbac-core": "workspace:*",
    "@repo/rbac-example": "workspace:*"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
